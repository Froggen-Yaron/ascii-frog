name: 🚀 ASCII-Frog Release

on:
  pull_request:
    branches: [ main ]
    types: [closed]

permissions:
  contents: write
  id-token: write

jobs:
  release:
    name: Release
    runs-on: self-hosted
    # Only run if PR was actually merged (not just closed)
    if: github.event.pull_request.merged == true
    
    steps:
    - name: 📥 Checkout code
      uses: actions/checkout@v4
      with:
        token: ${{ secrets.GITHUB_TOKEN }}
        ref: main
        fetch-depth: 0

    - name: 🔧 Configure Git
      run: |
        git config --global user.name "npm-version-bump"
        git config --global user.email "npm-version-bump@users.noreply.github.com"
        echo "🔀 PR #${{ github.event.pull_request.number }} was merged - preparing release"
        
    #############################################
    ########## Injected By Fly Desktop ##########

    - name: 🐸 JFrog Fly - Set as Default Registry for All Package Managers
      uses: Frog-Gen/fly-action@v1
      with:
        url: https://p1-flylnp1.jfrogdev.org

    ########## Injected By Fly Desktop ##########
    #############################################
        
    - name: 📤 NPM Release - @ascii-frog/backend
      run: |
        cd backend
        npm install
        npm version patch --no-git-tag-version
        BACKEND_VERSION=$(jq -r .version package.json)
        echo "BACKEND_VERSION=$BACKEND_VERSION" >> $GITHUB_ENV
        npm publish
        echo "✅ Backend package published successfully"
        
    - name: 📤 NPM Release - @ascii-frog/frontend  
      run: |
        cd frontend
        npm install
        npm version patch --no-git-tag-version
        npm run build
        FRONTEND_VERSION=$(jq -r .version package.json)
        echo "FRONTEND_VERSION=$FRONTEND_VERSION" >> $GITHUB_ENV
        npm publish
        echo "✅ Frontend package published successfully"

    
    - name: 📝 Commit version changes after successful npm publish
      run: |
        echo "✅ All publishing successful - now committing version changes"
        git add frontend/package.json backend/package.json
        git commit -m "chore: bump versions - frontend: $FRONTEND_VERSION, backend: $BACKEND_VERSION"
        git push origin main
        echo "🎉 Release complete with version bump committed!"

    - name: 🐳 Docker Build and Push - ascii-frog-app
      run: |
        VERSION=$(date +"%Y.%m.%d-%H.%M.%S")
        echo "📦 Building Docker version: $VERSION"
        docker build -t p1-flylnp1.jfrogdev.org/docker/ascii-frog-app:$VERSION .
        
        # Push with retries
        echo "🚀 Pushing Docker image with retries..."
        SUCCESS=false
        for i in {1..3}; do
          if docker push p1-flylnp1.jfrogdev.org/docker/ascii-frog-app:$VERSION; then
            SUCCESS=true
            break
          fi
          echo "❌ Push failed, retry $i..." && sleep $((i*3))
        done
        
        if [ "$SUCCESS" != "true" ]; then
          echo "❌ Failed to push after 3 attempts"
          exit 1
        fi
        
        echo "✅ Docker image pushed successfully"


