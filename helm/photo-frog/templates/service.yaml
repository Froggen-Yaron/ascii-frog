apiVersion: v1
kind: Service
metadata:
  name: {{ include "photo-frog.fullname" . }}
  labels:
    {{- include "photo-frog.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "photo-frog.selectorLabels" . | nindent 4 }}






