# Cursor Rules for ASCII Frog Project

## 🚫 CRITICAL GIT RULES - NEVER VIOLATE
- **NEVER COMMIT OR PUSH** unless the user explicitly asks to do so
- **NEVER PUSH TO MAIN BRANCH** - Always use feature branches
- **ALWAYS ASK PERMISSION** before any git operations (add, commit, push)
- **FOLLOW RELEASE.md WORKFLOW** - Use feature branches and PRs only

## 🚀 RELEASE RULES - MANDATORY CHECKLIST
When user says "release":
1. **FIRST**: Commit and push all current changes
2. **ANALYZE**: `git diff main..HEAD` to determine version type
3. **DETERMINE**: patch/minor/major based on commits with `feat:`, `fix:`, `BREAKING:`
4. **FINAL STEP - MANDATORY**: Run `npm version [patch|minor|major]` 
5. **PUSH VERSION**: `git push origin [branch]`
6. **VERIFY**: Ask user to confirm version bump is correct
7. **NEVER skip steps 4-5** - Version bump must be the final release step


## 🔧 TESTING & DEVELOPMENT RULES
- **NEVER RUN PROJECT AUTOMATICALLY** - Don't start servers or run tests unless explicitly asked
- **WHEN TASK IS COMPLETE, STOP** - After finishing code changes, don't automatically run/start/test
- **SUGGEST, DON'T EXECUTE** - Can suggest running `npm run dev` or testing but ask first
- **NO AUTO-TESTING** - Don't test functionality on your own unless requested
- **ASK PERMISSION** before running any development commands (npm start, npm test, etc.)

## Auto-fix suggestions
- When I have missing imports, suggest adding them at the top of the file
- When I ask to "fix imports" or "add missing imports", scan the file for undefined variables and suggest appropriate import statements

## 🚫 GIT COMMAND RULES - NEVER REQUIRE USER INPUT
- **NEVER run git commands that require user interaction**
- **ALWAYS use non-interactive flags:**
  - `git merge --no-edit` (never prompt for merge message)
  - `git pull --no-rebase` or `git pull --rebase` (specify strategy)
  - `git commit -m "message"` (always provide message)
  - `git push` (only use if no conflicts expected)
- **AVOID**: `git merge`, `git pull` without strategy, `git rebase` without flags
- **USER IS NOT AVAILABLE** to provide input during commands

## 🚨 CRITICAL GIT RULES - UPDATED FOR ABSOLUTE SAFETY
- **NEVER EVER use `git pull` without `--no-edit`** - IT WILL HANG!
- **NEVER EVER use `git merge` without `--no-edit`** - IT WILL HANG!
- **SAFE SYNC METHOD ONLY:**
  - `git fetch origin branch-name`
  - `git merge --no-edit origin/branch-name`
- **SAFE PULL METHOD ONLY:**
  - `git pull --no-rebase --no-edit origin branch-name`
- **FORBIDDEN COMMANDS:**
  - `git pull` (without --no-edit)
  - `git merge` (without --no-edit)
  - `git rebase` (without strategy)
- **REMEMBER: USER CANNOT PROVIDE INPUT - COMMANDS WILL HANG FOREVER**
