# Cursor Rules for ASCII Frog Project

## 🚫 CRITICAL GIT RULES - NEVER VIOLATE
- **NEVER COMMIT OR PUSH** unless the user explicitly asks to do so
- **NEVER PUSH TO MAIN BRANCH** - Always use feature branches
- **ALWAYS ASK PERMISSION** before any git operations (add, commit, push)
- **FOLLOW RELEASE.md WORKFLOW** - Use feature branches and PRs only

## Auto Import Rules
- When I use `colors.green()`, `colors.red()`, etc., automatically suggest adding: `const colors = require('colors');`
- When I use `express()`, automatically suggest: `const express = require('express');`
- When I use functions from our local modules, suggest the appropriate require statement
- Always prefer CommonJS `require()` syntax over ES6 imports for Node.js files

## Common Imports for this project:
- `const colors = require('colors');` - for terminal colors
- `const express = require('express');` - for web server
- `const { generateFrog, getAvailableTemplates } = require('../templates/templates');` - for frog templates
- `const { generateSpecificFrog, generateRandomFrog } = require('../services/frog-generator');` - for frog generation

## Auto-fix suggestions
- When I have missing imports, suggest adding them at the top of the file
- When I ask to "fix imports" or "add missing imports", scan the file for undefined variables and suggest appropriate require statements