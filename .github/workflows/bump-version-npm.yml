name: 🔖 Auto Version Bump on Merge

on:
  pull_request:
    branches: [ main ]
    types: [closed]

permissions:
  contents: write

jobs:
  version-bump:
    runs-on: linux-amd64
    # Only run if PR was actually merged (not just closed)
    if: github.event.pull_request.merged == true
    
    steps:
    - name: 📥 Checkout main branch
      uses: actions/checkout@v4
      with:
        token: ${{ secrets.GITHUB_TOKEN }}
        ref: main
        fetch-depth: 0

    - name: 🔧 Configure Git
      run: |
        git config --global user.name "npm-version-bump"
        git config --global user.email "npm-version-bump@users.noreply.github.com"

    - name: 📈 Auto patch version bump
      run: |
        echo "🔀 PR #${{ github.event.pull_request.number }} was merged - bumping patch version"
        
        echo "🔖 Bumping frontend version..."
        cd frontend
        npm version patch --no-git-tag-version
        FRONTEND_VERSION=$(jq -r .version package.json)
        echo "📊 Frontend version: $FRONTEND_VERSION"
        cd ..
        
        echo "🔖 Bumping backend version..."
        cd backend
        npm version patch --no-git-tag-version
        BACKEND_VERSION=$(jq -r .version package.json)
        echo "📊 Backend version: $BACKEND_VERSION"
        cd ..
        
        echo "📝 Committing version changes..."
        git add frontend/package.json backend/package.json
        git commit -m "chore: bump versions - frontend: $FRONTEND_VERSION, backend: $BACKEND_VERSION"
        
        echo "📤 Pushing version bump to main..."
        git push origin main
