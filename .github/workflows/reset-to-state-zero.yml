name: 🔄 Reset to State Zero

on:
  workflow_dispatch:

permissions:
  contents: write
  actions: write

jobs:
  reset-repository:
    name: Reset Repository to State Zero
    runs-on: linux-amd64
    
    steps:
    
    - name: 📥 Checkout code with full history
      uses: actions/checkout@v4
      with:
        fetch-depth: 0
        token: ${{ secrets.GITHUB_TOKEN }}
        
    - name: 🔄 Reset to State Zero
      run: |
        echo "🎯 Resetting repository to state zero..."
        echo "Target branch: main-backup"
        
        # Configure git
        git config --global user.name "GitHub Actions"
        git config --global user.email "reset-to-state-zero@github.com"
        
        # Fetch the main-backup branch
        git fetch origin main-backup
        
        # Reset to the main-backup branch (state zero)
        git reset --hard origin/main-backup
        
        # Force push to main branch to reset it
        echo "🚀 Force pushing reset to main branch..."
        git push --force origin HEAD:main

        
        echo "✅ Repository reset to state zero complete"
        
    - name: 🧹 Clean up workflow runs
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      run: |
        echo "🧹 Cleaning up old workflow runs..."
        
        # Get repository info
        REPO="${{ github.repository }}"
        
        # Get all workflow runs (keeping first 3)
        echo "📊 Fetching workflow runs..."
        RUNS=$(curl -s -H "Authorization: token $GITHUB_TOKEN" \
          "https://api.github.com/repos/$REPO/actions/runs?per_page=100" | \
          jq -r '.workflow_runs[3:] | .[].id')
        
        # Delete old workflow runs
        echo "🗑️ Deleting old workflow runs (keeping first 3)..."
        COUNT=0
        for run_id in $RUNS; do
          echo "Deleting run: $run_id"
          curl -s -X DELETE \
            -H "Authorization: token $GITHUB_TOKEN" \
            "https://api.github.com/repos/$REPO/actions/runs/$run_id" || echo "⚠️ Could not delete run $run_id"
          COUNT=$((COUNT + 1))
        done
        
        echo "✅ Cleaned up $COUNT workflow runs"
        echo "💡 Repository is now in state zero - ready for demo!"
        
    - name: 🎉 State Zero Complete
      run: |
        echo "🎯 State Zero Reset Complete!"
        echo ""
        echo "✅ Repository reset to main-backup branch state"
        echo "✅ Workflow runs cleaned (kept first 3)"
        echo "✅ All branches deleted (kept only main and main-backup)"
        echo ""
        echo "🚀 Repository is ready for demo!"
